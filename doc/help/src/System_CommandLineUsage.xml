<?xml version="1.0" encoding="UTF-8"?>
<refentry version="5.0" xml:id="CommandLine"
          xmlns="http://docbook.org/ns/docbook"
          xmlns:xlink="http://www.w3.org/1999/xlink"
          xmlns:xi="http://www.w3.org/2001/XInclude"
          xmlns:svg="http://www.w3.org/2000/svg"
          xmlns:m="http://www.w3.org/1998/Math/MathML"
          xmlns:html="http://www.w3.org/1999/xhtml"
          xmlns:db="http://docbook.org/ns/docbook">
  <indexterm>
    <primary>Command-Line Usage</primary>
  </indexterm>

  <indexterm>
    <primary><filename>GMAT</filename> command</primary>
  </indexterm>

  <refmeta>
    <refentrytitle>Command-Line Usage</refentrytitle>

    <refmiscinfo class="source">GMAT</refmiscinfo>

    <refmiscinfo class="manual">Resources</refmiscinfo>
  </refmeta>

  <refnamediv>
    <refname>Command-Line Usage</refname>

    <refpurpose>Starting the <filename>GMAT</filename> application from the
    command line</refpurpose>
  </refnamediv>

  <refsynopsisdiv>
    <cmdsynopsis>
      <command>GMAT</command>

      <arg rep="repeat"><replaceable>option</replaceable></arg>

      <arg><replaceable>script_file</replaceable></arg>
    </cmdsynopsis>

    <cmdsynopsis>
      <command>GMATConsole</command>

      <arg rep="repeat"><replaceable>option</replaceable></arg>

      <arg><replaceable>script_file</replaceable></arg>
    </cmdsynopsis>
  </refsynopsisdiv>

  <refsection>
    <title>Description</title>

    <para>The <literal>GMAT</literal> command starts the GMAT graphical
    interface. If run with no arguments, GMAT starts with the default mission
    loaded. If <literal><replaceable>script_file</replaceable></literal> is
    specified, and is a valid path to a GMAT script, GMAT loads the script and
    remains open, but does not run it. The <literal>GMATConsole</literal>
    command starts the GMAT console interface. See below for options supported
    by each interface.</para>
  </refsection>

  <refsection>
    <title>Options</title>

    <variablelist>
      <?dbfo list-presentation="blocks"?>

      <?dbhtml list-presentation="blocks"?>

      <varlistentry>
        <term><option>-b</option></term>

        <term><option>--batch</option></term>

        <listitem>
          <para>Runs multiple scripts listed in specified file.</para>
        </listitem>
      </varlistentry>

      <varlistentry>
        <term><option>-h</option></term>

        <term><option>--help</option></term>

        <listitem>
          <para>Start GMAT and display command-line usage information in the
          message window if using the GUI version, or in the terminal if using
          the console interface.</para>
        </listitem>
      </varlistentry>

      <varlistentry>
        <term><option>-l &lt;filename&gt;</option></term>

        <term><option>--logfile &lt;filename&gt;</option></term>

        <listitem>
          <para>Specify the log file (ignored in Console interactive
          mode.</para>
        </listitem>
      </varlistentry>

      <varlistentry>
        <term><option>-m</option></term>

        <term><option>--minimize</option></term>
		
        <listitem>
          <para>Start GMAT with a minimized interface.</para>
        </listitem>
      </varlistentry>

	  <varlistentry>
	    <term><option>-ns</option></term>
		
		<term><option>--no_splash</option></term>
		
		<listitem>
		  <para>Start GMAT without the splash screen showing.</para>
		</listitem>
	  </varlistentry> 
	  
      <varlistentry>
        <term><option>-r &lt;filename&gt;</option></term>

        <term><option>--run &lt;filename&gt;</option></term>

        <listitem>
          <para>Automatically run the specified script after loading.</para>
        </listitem>
      </varlistentry>

      <varlistentry>
        <term><option>--save &lt;filename&gt;</option></term>

        <listitem>
          <para>Saves current script (interactive mode only).</para>
        </listitem>
      </varlistentry>

      <varlistentry>
        <term><option>--start-server</option></term>

        <listitem>
          <para>Starts GMAT Server on start-up (ignored for Console).</para>
        </listitem>
      </varlistentry>

      <varlistentry>
        <term><option>-s &lt;filename&gt;</option></term>

        <term><option>--startup_file &lt;filename&gt;</option></term>

        <listitem>
          <para>Specify the startup file (ignored in Console interactive
          mode).</para>
        </listitem>
      </varlistentry>

      <varlistentry>
        <term><option>--summary</option></term>

        <listitem>
          <para>Writes command summary (interactive mode only).</para>
        </listitem>
      </varlistentry>

      <varlistentry>
        <term><option>--verbose </option></term>

        <listitem>
          <para>Dump info messages to screen during run (default is
          on).</para>
        </listitem>
      </varlistentry>

      <varlistentry>
        <term><option>-v</option></term>

        <term><option>--version</option></term>

        <listitem>
          <para>Start GMAT and display version information in the message
          window.</para>
        </listitem>
      </varlistentry>

      <varlistentry>
        <term><option>-x</option></term>

        <term><option>--exit</option></term>

        <listitem>
          <para>Exit GMAT after running the specified script. If specified
          with only a script name (i.e. NO –run option), GMAT simply opens and
          closes.</para>
        </listitem>
      </varlistentry>
    </variablelist>
  </refsection>

  <refsection>
    <title>Precedence Rules</title>

    <para>Some file locations, the log file for example, can be set in
    multiple locations. The precedence rules are as follows. Command line
    settings have the highest precedence, and those values are always used if
    set. The second precedence is taken by script level settings, for example,
    <literal>GmatGlobal.LogFile = C:\myLog.txt</literal>. Finally, if no other
    method is set, the value in the startup file is used.</para>

    <para>There are additional precedence rules that apply when the startup
    file is configured to use <literal>RUN_MODE = TESTING</literal>. In that
    case, the log file name from the startup file has precedence, and the
    output path can be overwritten by settings avialalble in the GUI
    <literal>Set File Paths</literal> option in the <literal>File</literal>
    menu, or in the <literal>Run Scripts</literal> option avialable in the
    <literal>Scripts</literal> menu in the <literal>Resource
    Tree</literal>.</para>
  </refsection>

  <refsection>
    <title>Examples</title>

    <informalexample>
      <para>Start GMAT and run the script
      <filename>MyScript.script</filename>:</para>

      <para><userinput>GMAT MyScript.script</userinput></para>
    </informalexample>

    <informalexample>
      <para>Run a script with the interface minimized, and exit
      afterwards:</para>

      <para><userinput>GMAT --minimize --exit
      MyScript.script</userinput></para>
    </informalexample>
  </refsection>
</refentry>
