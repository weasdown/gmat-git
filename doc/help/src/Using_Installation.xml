<?xml version="1.0" encoding="UTF-8"?>
<section version="5.0" xml:id="Installation"
         xmlns="http://docbook.org/ns/docbook"
         xmlns:xlink="http://www.w3.org/1999/xlink"
         xmlns:xi="http://www.w3.org/2001/XInclude"
         xmlns:svg="http://www.w3.org/2000/svg"
         xmlns:m="http://www.w3.org/1998/Math/MathML"
         xmlns:html="http://www.w3.org/1999/xhtml"
         xmlns:db="http://docbook.org/ns/docbook">
  <title>Installation</title>

  <indexterm>
    <primary>Installation</primary>
  </indexterm>

  <para>Application bundles are available on the GMAT SourceForge project
  page, located at <uri>https://sourceforge.net/projects/gmat</uri>.</para>

  <para>The following packages are available for the major platforms:</para>

  <informaltable colsep="0" frame="topbot">
    <tgroup cols="3">
      <colspec align="center"/>

      <thead>
        <row>
          <entry align="center">Operating System</entry>

          <entry align="center">Binary bundle</entry>

          <entry align="center">Source code</entry>
        </row>
      </thead>

      <tbody>
        <row>
          <entry>Windows (10)</entry>

          <entry align="center" valign="middle">✔</entry>

          <entry align="center" valign="middle">✔</entry>
        </row>

        <row>
          <entry>macOS</entry>

          <entry align="center" valign="middle">✔</entry>

          <entry align="center" valign="middle">✔</entry>
        </row>

        <row>
          <entry>Linux</entry>

          <entry align="center" valign="middle">✔</entry>

          <entry align="center" valign="middle">✔</entry>
        </row>
      </tbody>
    </tgroup>
  </informaltable>

  <simplesect>
    <title>Binary Bundle</title>

    <para>A binary bundle is available on Windows as a
    <filename>.zip</filename> archive. To use it, unzip it anywhere in your
    file system, making sure to keep the folder structure intact. To run GMAT,
    run the <filename>GMAT\bin\GMAT.exe</filename> executable in the extracted
    folder.</para>

    <para>The MacOS binary bundle is available as a signed DMG file. To use it, 
    install the image in either the global Applications folder (requires 
    administrative access) or in your user account's Applications folder. To run 
    the production quality GMAT console application, open a terminal, cd to the 
    <filename>GMAT R2022a/bin/</filename> folder, and run the GmatConsole-R2022a 
    application.  To run the Beta level GMAT GUI, run the 
    <filename>GMAT R2022a/bin/GMAT-R2022a_Beta.app</filename> application in the 
    extracted folder.</para>

    <para>The Linux builds for GMAT are packaged in compressed tarballs.  
    Download either the Red Hat or Ubuntu tarball, extract the contents in a 
    convenient location (preserving the file system structure), and GMAT will be 
    available for use. To run the production quality console application, open a 
    terminal, cd to your <filename>GMAT/R2022a/bin</filename> folder,
    and run the GmatConsole application. The beta quality GMAT GUI, 
    <filename>GMAT/R2022a/bin/GMAT_Beta</filename> can also be run from this
    folder. (Note that the GMAT Linux builds follow the Linux custom of placing 
    support libraries in a lib folder parallel to the bin folder. You may need 
    to load that folder by setting LD_LIBRARY_PATH as part of your launch 
    process.)</para>

  </simplesect>

  <simplesect>
    <title>Source Code</title>

    <para>GMAT is available as a platform-independent source code bundle. See
    the <link xlink:href="http://gmatcentral.org">GMAT Wiki</link> for
    compiling instructions.</para>

    <para>The release snapshot of the GMAT code is available from the Git
    repository at SourceForge:
    <literallayout><uri>https://git.code.sf.net/p/gmat/git</uri></literallayout></para>

    <para>There are tags available in the repository for reach release.</para>
  </simplesect>
</section>
